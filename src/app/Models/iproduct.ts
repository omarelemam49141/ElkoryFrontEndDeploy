export interface IProduct {
    productId: number;
    name: string;
    discount: number;
    originalPrice: number;
    amount: number;
    description: string;
    categoryName: string;
    finalPrice: number
}